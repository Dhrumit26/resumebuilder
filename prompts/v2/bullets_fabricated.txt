You invent ONE coherent mid-level engineering role for this posting. Facts are ignored.
Write exactly {{COUNT}} bullets for {{BLOCK_LABEL}}.

Every bullet must answer ALL THREE — or it fails:
1. WHAT happened — the concrete change on the system
2. HOW — the tech / method that produced it
3. WHY / RESULT — the measured impact (number, from→to, or clear consequence)

Length: fill MORE than one resume line and LESS than two (about 22–36 words,
~115–200 characters). Short one-liners next to dense Intuit bullets look empty.

## GOLD — copy the SHAPE (what + how + why on every line)

AI/LLM/agentic (Python + AWS):
1. Built Python RAG retrieval APIs for an internal agent console that answers support questions over product docs on AWS, cutting first-response draft time from 12 minutes to under 4.
2. Cut p95 multi-agent query latency on that console from 1.8s to 1.1s by rewriting the embedding lookup path and batching model inference calls under load.
3. Shipped tool-calling orchestration for those agents so they could read tickets and draft replies in-console, raising successful auto-drafts from 40% to 72% of sampled threads.
4. Raised pytest coverage on those LLM workflow handlers from 48% to 82%, which cut retrieval regressions reaching staging by more than half over six weeks.
5. Deployed that same retrieval service with Docker on AWS so staging matched production inference settings, shrinking release dry-runs from 3 hours to 45 minutes.

Frontend (TypeScript + React):
1. Built a TypeScript React ops dashboard for internal support that surfaces live ticket queues and SLA breach alerts on AWS, replacing a spreadsheet workflow used by 12 agents.
2. Cut the slowest dashboard filter path from 900ms to 320ms by memoizing list virtualization and trimming React re-renders on the hottest queue views.
3. Raised Jest and React Testing Library coverage on that dashboard from 40% to 78%, cutting UI regressions shipped to staging by roughly 40% sprint over sprint.
4. Wired the same dashboard to REST ticket APIs with typed clients so broken payload shapes failed in CI, dropping related production incidents from 6 per month to 1.
5. Shipped the dashboard behind a feature flag on AWS and rolled to 100% of internal users over two weeks with zero rollback after the canary week.

## INSTANT FAIL
- Missing WHAT, HOW, or RESULT on any bullet
- Under ~22 words / one short line (too thin vs Intuit)
- Over ~36 words / past two lines
- No digit on a bullet (every line needs a metric or counted result)
- Collaborated / Monitored / Enhanced code quality / seamless / zero-metric process lines
- Language or cloud scatter

## METRICS
- EVERY bullet carries a real number (%, time, count, from→to).
- At least TWO bullets use from→to (latency, coverage, time, cost, volume).
- At most three "hard" scoreboard metrics; others can be counts/rollouts — still numeric.
- Prefer from→to. Never "improving performance" or "reducing bugs" with no figure.

## SPINE — mandatory when listed
{{JD_SPINE}}
Related: {{JD_CONCEPTS}}
Primary language: {{PRIMARY_LANGUAGE}} (name in bullet 1 or 2). Cloud: {{PRIMARY_CLOUD}} only.
If RAG/agent/embeddings/LLM/inference/orchestration are listed, each MUST appear as work on the system.

## CRAFT
- One sentence. Outcome first, then how, then why/result. 22–36 words.
- Verbs: Built, Shipped, Cut, Raised, Migrated, Rewrote, Fixed, Reduced, Defined, Deployed, Covered, Wired.
- Never: Collaborated, Facilitated, Conducted, Participated, Helped, Ensured, Boosted, Monitored, leveraged, utilized, robust, seamless.
- Bullets 2–{{COUNT}} refer back ("that console", "the same dashboard", "those agents").
- Plain prose. No LaTeX, no \\textbf, no markdown. Write % plainly.

## THIS JOB POSTING
Title: {{JD_TITLE}}
Domain: {{JD_DOMAIN}}
Practices: {{JD_PRACTICES}}
Tools: {{JD_TOOLS}}
Worth surfacing: {{PLACEMENT_KEYWORDS}}

## THIS BLOCK
{{BLOCK_KIND}}: {{BLOCK_LABEL}}{{TENURE}}
{{FLEX_RULE}}

## OUTPUT
{"bullets": ["...", "..."]}  — exactly {{COUNT}} strings, plain text only.
{{FIX_BLOCK}}
