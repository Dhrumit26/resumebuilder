You invent ONE coherent mid-level engineering role for this posting. Facts are ignored.
Write exactly {{COUNT}} bullets for {{BLOCK_LABEL}}.

Recruiters compare this block to Intuit below it. If Clerxi has no numbers and Intuit
has five, you failed. Density must match: concrete system + stack + measured change.

## GOLD — copy the SHAPE (not the words)

AI/LLM/agentic JD (Python + AWS):
1. Built Python RAG retrieval APIs for an internal agent console that answers support questions over product docs on AWS.
2. Cut p95 multi-agent query latency on that console from 1.8s to 1.1s by rewriting the embedding lookup and inference batching.
3. Shipped tool-calling orchestration for those agents so they could read tickets and draft replies without leaving the console.
4. Raised pytest coverage on those LLM workflow handlers from 48% to 82% so retrieval regressions stopped shipping into the agent loop.
5. Deployed that same retrieval service with Docker on AWS, cutting staging-to-prod config drift from hours to minutes before each release.

Frontend JD (TypeScript + React):
1. Built a TypeScript React ops dashboard for internal support that surfaces live ticket queues and SLA breach alerts on AWS.
2. Cut the slowest dashboard filter path from 900ms to 320ms by memoizing list virtualization and trimming re-renders.
3. Raised Jest + React Testing Library coverage on that dashboard from 40% to 78%, cutting UI regressions shipped to staging.
4. Wired the same dashboard to REST ticket APIs with typed clients so broken payload shapes failed in CI instead of production.
5. Shipped the dashboard behind a feature flag on AWS, rolling to 100% of internal users over two weeks with zero rollback.

## INSTANT FAIL — do not write these
- "Collaborated with the team…" / "Monitored telemetry…" / "Enhanced code quality…" / "ensuring seamless…"
- Zero metrics across five bullets while Intuit has many
- Keyword list with no system ("React, Jest, Kubernetes, AWS" costume)
- Bare "by 25%" with no from→to
- Language scatter or multi-cloud

## METRICS (mechanical)
- At least TWO bullets must carry a real number.
- At least ONE must be from→to (latency, coverage, time, cost).
- At most THREE hard metrics in the whole block — other bullets = concrete delivery with tools, still specific.
- Prefer: from 1.8s to 1.1s, from 40% to 78%, from 2 weeks to 4 days. Not: "reducing bugs", "improving performance".

## SPINE — mandatory when listed
{{JD_SPINE}}
Related: {{JD_CONCEPTS}}
Primary language: {{PRIMARY_LANGUAGE}} (name in bullet 1 or 2). Cloud: {{PRIMARY_CLOUD}} only.
If RAG/agent/embeddings/LLM/inference/orchestration are listed, each MUST appear as work on the system.

## CRAFT
- XYZ: outcome first, then how. 18–30 words. One sentence.
- Verbs: Built, Shipped, Cut, Raised, Migrated, Rewrote, Fixed, Reduced, Defined, Deployed, Covered, Wired.
- Never: Collaborated, Facilitated, Conducted, Participated, Helped, Ensured, Boosted, Monitored (alone), leveraged, utilized, robust, seamless.
- Bullets 2–{{COUNT}} refer back ("that console", "the same dashboard", "those agents").
- Plain prose. No LaTeX, no \\textbf, no markdown. Write % plainly.

## THIS JOB POSTING
Title: {{JD_TITLE}}
Domain: {{JD_DOMAIN}}
Practices: {{JD_PRACTICES}}
Tools: {{JD_TOOLS}}
Worth surfacing: {{PLACEMENT_KEYWORDS}}

## THIS BLOCK
{{BLOCK_KIND}}: {{BLOCK_LABEL}}{{TENURE}}
{{FLEX_RULE}}

## OUTPUT
{"bullets": ["...", "..."]}  — exactly {{COUNT}} strings, plain text only.
{{FIX_BLOCK}}
